export const skeleton = {
    weatherDiv : document.querySelector('.weather') as HTMLElement,
    degreeDiv : document.querySelector('.degree') as HTMLElement,
    date : document.querySelector('.date') as HTMLElement,
    cityDiv : document.querySelector('.city') as HTMLElement,
    locationInput: document.querySelector('.location') as any,
    weatherImg : document.querySelector('.weatherIcon') as HTMLImageElement,
    searchButton : document.querySelector('.confirm') as HTMLElement,
    body : document.querySelector('body') as HTMLElement
}